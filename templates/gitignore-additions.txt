# envibe - Add these to your .gitignore

# Original .env files (contain secrets)
.env
.env.*
!.env.example
!.env.manifest.yaml
.envibe/

# AI-generated view (regenerated, no need to commit)
.env.ai

# Keep these committed:
# .env.manifest.yaml  (access control rules)
# .env.example        (template for developers)
